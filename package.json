{
  "name": "ha-vestaboard-composer",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "screenshot": "node scripts/capture-screenshot.js",
    "deploy": "npm run build && node scripts/deploy.js",
    "dev-deploy": "node scripts/dev-deploy.js",
    "preversion": "npm test",
    "version": "npm run build && git add ha-vestaboard-composer.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "serve": "^14.0.0"
  }
}
